SCOPE: Collapse the entire RECOMMENDED MODEL section into a single-line metadata strip inside the I. INPUT section, and renumber the BLUEPRINTS section from III to II.

CURRENT STATE
- I. INPUT section: shows the prompt the user submitted.
- II. RECOMMENDED MODEL section: dedicated centered block with heading lockup, model name in large terracotta serif, RECOMMENDED · DETERMINISTIC FALLBACK tag, description paragraph, ALTERNATIVES sub-label, and an alternatives card. Consumes massive vertical space for what is effectively three facts.
- III. BLUEPRINTS section.

REQUIRED CHANGE

1) DELETE THE STANDALONE RECOMMENDED MODEL SECTION
Remove the entire "II Recommended model" heading lockup, the large terracotta model name, the RECOMMENDED tag, the description paragraph, the ALTERNATIVES sub-label, and the alternatives card from their current location. Remove all the surrounding container divs / wrappers / padding too — no orphan whitespace where the section used to be.

2) ADD A SINGLE-LINE MODEL STRIP INSIDE THE I. INPUT SECTION

At the bottom of the I. INPUT section, directly below the displayed user prompt and above the section's bottom padding, insert a single horizontal metadata row:

   MODEL  openai / gpt-5.5  ·  RECOMMENDED  ·  FALLBACK  gemini / gemini-2.5-flash  (SHADOW ONLY)

Treatment:
- Single line, left-aligned, sits flush with the rest of the I. INPUT content (same left edge as the prompt text above).
- Top: 16px gap from the prompt text above it.
- Top hairline: 1px solid --border-hairline running above the strip, width matches the input section's content column.
- 12px padding between the hairline and the strip text.

Strip content typography:
- "MODEL" label: mono caps 10px, --text-muted, letter-spacing 0.12em.
- "openai / gpt-5.5" value: mono 12px (JetBrains Mono), --accent terracotta.
- "RECOMMENDED" pill: mono caps 10px, --text-muted.
- "FALLBACK" label: mono caps 10px, --text-muted.
- "gemini / gemini-2.5-flash" value: mono 12px, --text-body (NOT terracotta — keeps emphasis on the recommended one).
- "(SHADOW ONLY)" note: mono caps 9px, --text-faint, in parentheses.
- Middle dot separators (·) between major segments: --text-muted, 12px horizontal margin on each side.

If the line gets too long at narrower viewports (< 1024px), wrap to a second line — but the first line should always start with "MODEL openai / gpt-5.5 · RECOMMENDED".

3) DELETE THE DESCRIPTION TEXT ENTIRELY
The sentence "Use the teacher/default model or deterministic fallback until a student model passes the latest golden prompt calibration run." is removed in this change. It's internal documentation that doesn't belong in the user-facing UI. If we need to surface it later, we'll add a small "(?)" tooltip in a future pass — not in this change.

4) RENUMBER SECTIONS
The BLUEPRINTS section is currently labeled "-- III. BLUEPRINTS". Change to "-- II. BLUEPRINTS" since RECOMMENDED MODEL no longer occupies the II slot.

If the side section navigator (01 Blueprint / 02 License / 03 Eval plan / 04 Source link) references roman numerals anywhere, double-check none of those need updating — the side nav uses 01/02/03/04 numbering, not roman, so it should be unaffected.

WHAT NOT TO TOUCH
- The I. INPUT section's existing prompt-display treatment — unchanged.
- The BLUEPRINTS section's tab strip, export row, blueprint content, and gating behavior — unchanged.
- The side navigator — unchanged.
- The hero, the textarea, and the landing page — unchanged.

VERIFICATION
1. Reload results page. Confirm the standalone "Recommended model" section is GONE — no heading, no large terracotta model name, no centered description, no alternatives card in its previous location.
2. Confirm the I. INPUT section now ends with a single-line metadata strip showing: MODEL openai / gpt-5.5 · RECOMMENDED · FALLBACK gemini / gemini-2.5-flash (SHADOW ONLY), separated from the prompt above by a hairline.
3. Confirm "openai / gpt-5.5" reads in terracotta while "gemini / gemini-2.5-flash" reads in body color, so the recommended one is visually weighted.
4. Confirm the BLUEPRINTS section header now reads "-- II. BLUEPRINTS" instead of "-- III. BLUEPRINTS".
5. Confirm the page is visibly shorter — the ~500px the old section consumed should be reclaimed.
6. Resize below 1024px. Confirm the metadata strip wraps gracefully to a second line if needed without breaking layout.
7. Inspect the DOM. Search for "Use the teacher/default model" — that string should no longer exist on the page.