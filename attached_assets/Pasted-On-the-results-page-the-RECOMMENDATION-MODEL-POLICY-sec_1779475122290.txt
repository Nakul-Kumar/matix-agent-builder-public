On the results page, the RECOMMENDATION MODEL POLICY section currently shows the chosen model as a single mono caps pill plus a "deterministic fallback" pill and a small shadow-model note. Rebuild it to highlight the recommended model and show the alternatives clearly.

New layout:
1. Section header (centered + enlarged, matching the treatment from the previous prompt):
   - Label line: "—— II. RECOMMENDED MODEL" in JetBrains Mono caps 14px, letter-spacing 0.16em, color #6E6359, center-aligned.
   - Subtitle: blank — no subtitle for this section. Just the label.
   - Hairline divider below, same as the previous section header.

2. Beneath the header, render the recommended model as a prominent centered display:
   - The recommended model name in Fraunces 36px weight 600, color terracotta primary #C2674A, center-aligned. Format: "openai / gpt-5.5" (or whatever the chosen model is, exactly as the backend returns it).
   - 8px below, a small mono caps line in 11px, color #A8998C, center-aligned: "RECOMMENDED · DETERMINISTIC FALLBACK"
   - 24px below, the existing policy explanation paragraph ("Use the teacher/default model or deterministic fallback until a student model passes the latest golden prompt calibration run.") — set in sans 14px, color #A8998C, center-aligned, max-width 640px, line-height 1.5.

3. Beneath the recommended model display, render the ALTERNATIVES row:
   - 48px of vertical space between the policy paragraph and this row.
   - A small mono caps header: "—— ALTERNATIVES" centered, 10px, letter-spacing 0.16em, color #6E6359.
   - 16px below, a horizontal row of alternative model pills, centered, with gap 12px.
   - Each alternative pill: rectangular, padding 8px 16px, border 1px solid #2A2420, border-radius 4px, background #14181C, no fill.
     - Inside each pill: model name in JetBrains Mono 12px, letter-spacing 0.08em, color #A8998C.
     - Beneath the model name (inside the pill), in mono 9px caps, letter-spacing 0.14em, color #6E6359: the model's role/status (e.g. "SHADOW ONLY" / "FALLBACK" / "AVAILABLE").
   - Use the existing data for alternatives. If the response currently exposes only the primary plus the shadow model ("Gemini / Gemini-2.5-Flash: shadow only"), render those two as pills. If the response includes additional alternatives, render all of them.

4. Visual emphasis:
   - The recommended model is the only terracotta-colored text on the page in this section.
   - Alternative pills use neutral muted text — clearly secondary.
   - Hovering an alternative pill: border becomes #C2674A and the model name inside becomes #EDE6DE. Click does nothing for now (alternatives are display-only).

5. Preserve the existing data — do not invent models that aren't in the response. If the backend returns only one model, render it as the recommended display and show an empty alternatives row with the small note "—— NO ALTERNATIVES CONFIGURED" in mono caps 10px color #6E6359 centered.

Apply this treatment ONLY to the RECOMMENDED MODEL section. Other sections stay as-is.

Preserve all other behavior, data, and content.