Remove the current single decorative blob in the right column and replace it with a multi-shape organic composition that frames the entire viewport edges, leaving the center content area clear. The reference aesthetic is a layered illustration of overlapping organic shapes plus thin curving line accents anchored to the corners — NOT a single feature shape.

Implementation:

1. Delete the existing single blob SVG and the "MATIX · COCKPIT · V0.1" caption that sat beneath it from the right column. The right column becomes empty content-wise; the decoration moves to the page level.

2. Add a new full-page decoration layer:
   - Container: position fixed, inset 0, z-index 0 (behind all content). pointer-events: none.
   - Inside it, render ONE single inline SVG with viewBox set to the viewport dimensions (use 100vw x 100vh, or a 1600x900 viewBox scaled with preserveAspectRatio="xMidYMid slice").
   - The SVG contains six organic shapes + three thin curving line accents, placed at the viewport edges.

3. Shape composition — six organic blobs, each anchored to a different region of the viewport. Each shape is an irregular custom path (≥8 control points, deliberately asymmetric — NOT circles or ellipses). Specifications:
   - Shape A — top-left corner: large, clips off the top and left edges, fill terracotta deep (#9A4B33), opacity 0.85, occupies roughly 28% viewport width × 30% viewport height.
   - Shape B — top-right corner: medium, clips off the top and right edges, fill terracotta primary (#C2674A), opacity 0.9, ~22% viewport width × 26% viewport height.
   - Shape C — mid-left edge: small, anchored to left edge below shape A, fill terracotta soft (#E08B6B), opacity 0.75, ~14% width × 18% height.
   - Shape D — bottom-right corner: large, clips off the bottom and right edges, fill terracotta deep (#9A4B33), opacity 0.85, ~30% width × 28% height.
   - Shape E — bottom-left corner: medium, clips off the bottom and left edges, fill terracotta primary (#C2674A), opacity 0.9, ~24% width × 24% height.
   - Shape F — small accent shape near top-center: small, fully visible (not clipped), fill terracotta soft (#E08B6B), opacity 0.7, ~10% width × 12% height, positioned around x=55% y=8%.
   - Where shapes overlap (e.g. shape A overlapping with shape C, or D with E), allow their colors to mix naturally via opacity. Use mix-blend-mode: multiply on each shape so the overlaps darken into richer terracotta tones — this gives the composition real depth.

4. Line accents — three thin curving lines that snake through the composition:
   - Stroke: terracotta soft (#E08B6B), stroke-width 2, fill none, opacity 0.55.
   - Line 1: curves from upper-right area down toward the middle-left, ending past the viewport edge. A long sweeping S-curve.
   - Line 2: arcs across the top portion of the viewport from left edge to right edge, passing over shape F.
   - Line 3: a smaller curl in the lower-left area near shape E.
   - Use SVG <path> with cubic bezier curves — at least two control-point pairs per line for smooth organic curvature. No straight segments.
   - Each line clips off the viewport edges where it exits, suggesting continuation past the frame.

5. Center clearance:
   - The central rectangular region of the viewport (from 20% to 80% horizontally and 15% to 85% vertically) must remain unobstructed by shape fills. Shapes can clip INTO this region from their corners but their centers of mass stay outside it. This is non-negotiable — content readability depends on it.

6. Subtle texture:
   - Apply a single SVG filter combining feTurbulence (baseFrequency 0.02, numOctaves 2) and feDisplacementMap (scale 4) to all six shapes so edges have a slight hand-drawn irregularity. Apply it once at the <g> level, not per shape.

7. Responsive behavior:
   - At viewport widths ≥1280px: all six shapes + three lines visible as described.
   - 1024px–1279px: hide shape F and line 3 to reduce visual density.
   - 768px–1023px: keep only shapes A, B, D, E (the four corners) and line 2. Reduce all shape opacities to 0.5.
   - Below 768px: keep only shapes A and D (top-left and bottom-right diagonal anchors) at opacity 0.4. Hide all lines.

8. Z-index discipline:
   - Decoration layer: z-index 0.
   - Page content (the