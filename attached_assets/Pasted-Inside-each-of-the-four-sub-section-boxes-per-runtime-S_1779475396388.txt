Inside each of the four sub-section boxes per runtime (SKILLS, MCPS, FILES, REASONS), the box header label currently renders at 11px in muted color #6E6359, which makes it read as a footnote rather than a section title. Make it visually prominent.

Changes to each box header label only — do NOT change the individual cards inside the boxes.

1. Label text:
   - "—— SKILLS" / "—— MCPS" / "—— FILES" / "—— REASONS"
   - Font family: JetBrains Mono (unchanged).
   - Size: 11px → 16px.
   - Letter-spacing: stays 0.16em.
   - Color: #6E6359 → #EDE6DE (text primary, the "white" the user wants).
   - Font weight: 500 (slightly heavier than the default 400 so it carries the size).
   - Keep the leading "—— " em-dashes.

2. The "N ITEMS" count line directly beneath the label:
   - Stays in JetBrains Mono caps.
   - Size: stays 10px.
   - Color: stays #6E6359 (muted) so it still reads as secondary metadata, contrasting with the now-prominent label above it.
   - Letter-spacing: stays 0.16em.
   - Position: stays directly beneath the label with the existing small gap.

3. The 1px hairline divider beneath the count line, and the 16px gap before the scroll content begins — both unchanged.

4. This treatment applies to ALL twelve sub-section boxes on the results page (four per runtime × three runtimes — SKILLS / MCPS / FILES / REASONS for Codex, Claude Code, and OpenClaw). Apply consistently.

5. Do NOT change:
   - The individual Result Cards' category labels inside each box (SKILL / MCP / FILE / REASON in terracotta primary at 10px). Those stay as they are — they're the per-card labels.
   - The Result Cards' titles, previews, footnotes.
   - The scroll behavior, rail, brackets, mask fade on the box.
   - The 2x2 grid layout of the boxes.
   - The runtime header card above each 2x2 grid.

Verification:
- The four box headers in each runtime block (SKILLS / MCPS / FILES / REASONS) now read as proper section titles — clearly visible at a glance, set in white text at 16px mono caps.
- The "N ITEMS" count line beneath each label remains muted, so the visual hierarchy is: prominent label, secondary count.
- Inside each box, the individual card labels (SKILL / MCP / FILE / REASON) remain in their existing 10px terracotta treatment — they're the per-card labels and should not compete with the box header above.