Add a non-transparent background to the six prompt cards in the START FROM scroll container. The new fill should be deep moss green — a desaturated dark olive that pairs naturally with terracotta without competing with it. The page background stays warm near-black; only the card surfaces change.

Color additions to the theme tokens:
- Card surface (new token): #1A211C — deep moss, reads almost black with a green undertone.
- Card hairline (new token): #232A24 — slightly green-tinted hairline that matches the new card surface tonally.

Apply these to the prompt cards:

1. Card background: change from transparent to #1A211C.

2. Card border: change from #2A2420 (warm hairline) to #232A24 (moss hairline). 1px solid, no change in width or radius.

3. On hover: the terracotta primary border (#C2674A) and left-edge inset stay exactly as they are. The hover state actually reads stronger now because moss + terracotta are complementary colors — the hover pops harder against the moss fill than it did against transparent.

4. Inside the card, all text colors stay the same:
   - Category label remains terracotta primary #C2674A.
   - Title remains text primary #EDE6DE.
   - Preview remains text secondary #A8998C.
   - Footnote remains text muted #6E6359.
   All four remain fully legible against the new moss fill — verify by viewing the card in the browser.

5. Do NOT change:
   - The page background (#0B0A0A).
   - Any other surface on the page (textarea, footer, top-right indicator, decoration shapes, lines).
   - The mask fade on the scroll container.
   - The custom rail or corner brackets (those stay as-is on the warm hairline color #2A2420 because they decorate the container, not the cards).
   - Any text color, font, or size.

6. The new moss tokens (#1A211C and #232A24) are LOCAL to the prompt cards. They are not added to the global theme palette. They do not appear anywhere else on the page or in the results view. Other components keep their existing colors.

7. Verification:
   - Each of the six cards now has a visibly distinct dark moss surface that separates it from the warm near-black page background.
   - The cards still feel cohesive with the page — the moss is dark enough not to fight the terracotta accents or the warm dark base.
   - Hover state still works: terracotta border + left-edge inset pops more strongly than before.
   - Category labels remain crisp terracotta against moss (good contrast since the colors are complementary).
   - The decoration shapes and line accents still read correctly behind the cards (they're behind, the moss cards are opaque so the decoration is hidden inside the card footprint — that's expected).