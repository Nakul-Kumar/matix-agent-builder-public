SCOPE: Small framing tweak to the START FROM scroll container. The cards are example prompts that populate the textarea when clicked, but right now they read as features or primary options. Add a few light, complementary cues so it's obvious these are examples — not extreme changes, just clearer signposting.

THREE SMALL CUES — apply all three so they reinforce each other

1) RENAME THE CONTAINER HEADER
Change the existing "-- START FROM" mono caps header to one of these (pick the first):
   -- TRY AN EXAMPLE
The "START FROM" label is too cool/architectural — readers parse it as a feature name. "TRY AN EXAMPLE" reads as an action and immediately frames what follows as samples.

2) ADD A ONE-LINE HELPER UNDER THE HEADER
Directly under the renamed header, add a single line of helper text (Fraunces italic 13px, --text-muted color). Content:
   Click any card to drop it into your prompt — then edit freely.

This sentence does the actual work of explaining the affordance: the cards are clickable templates, the textarea is editable after. No icon needed.

Spacing: 6px gap between the "-- TRY AN EXAMPLE" header and this helper line, then the existing gap before the scroll container.

3) ADD A "USE THIS →" HOVER AFFORDANCE ON EACH CARD
On hover, reveal a small "Use this →" label in the bottom-right corner of the card (mono caps 10px, --accent terracotta color, 0 → 1 opacity transition over 120ms). When the cursor leaves, it fades back out.

Position: absolute, bottom: 12px, right: 14px on the card. Make sure it doesn't overlap the existing footnote row ("4 TOOLS · GITHUB · POSTGRES" etc.) — push the footnote left when the affordance appears, or place the affordance on a row above the footnote.

If the affordance overlaps awkwardly with the existing footnote tags on hover, just put the "Use this →" inline as a new last item in the footnote row (still terracotta, still mono caps 10px) instead of absolute-positioning it. Either approach is fine — pick whichever reads cleaner in your existing card layout.

Reduced motion: if prefers-reduced-motion is set, show "Use this →" at full opacity always (no fade), so the cue is still there without animation.

WHAT NOT TO DO (constraint reminders)
- Do not add an "EXAMPLE" badge to every card — that's too heavy and visually noisy.
- Do not change the card category labels (ENGINEER, RESEARCH, SUPPORT, DATA, OPS, CONTENT) — those stay as the primary card tag.
- Do not change the L-bracket corners, scroll rail, or any other container chrome.
- Do not change card spacing, padding, height, or background.

VERIFICATION
1. Reload landing page. Confirm the header now reads "-- TRY AN EXAMPLE" and the italic helper line sits cleanly below it.
2. Hover each card. Confirm "Use this →" appears in terracotta, fades smoothly, and doesn't collide with the existing footnote tags.
3. Click a card. Confirm it still drops the example prompt into the textarea (functionality unchanged).
4. Tab through with the keyboard. The card focus state should also trigger the "Use this →" cue (keyboard parity with hover).
5. With pr