SCOPE: Two related fixes to the RECOMMENDED MODEL section. (1) Promote the section title so it reads as a real heading, not a small mono-caps label. (2) Compact the vertical spacing throughout — the section currently consumes far more screen height than its content warrants.

CHANGE 1 — PROMOTE THE SECTION TITLE

Current title: small mono-caps "-- II. RECOMMENDED MODEL" centered above the model card. It reads as a label, not a section header.

Replace with a proper editorial heading lockup, left-aligned at the top of the section:

   II  Recommended model

Specifically:
- The numeral "II" sits in mono caps 11px, color --accent (terracotta), letter-spacing 0.12em, vertically aligned with the heading's cap-height. Acts as a kicker.
- "Recommended model" sits next to it as a Fraunces serif heading, weight 600, 32px, color --text-primary. NOT centered — left-aligned.
- 16px gap between the "II" kicker and the heading text.
- A 1px hairline rule (--border-hairline) running BELOW the lockup, full width of the content container. 16px gap between the heading and the hairline.

Remove the existing "-- II. RECOMMENDED MODEL" mono-caps centered label entirely. The new lockup replaces it.

Do NOT apply this promotion to the other section headers (I. INPUT, III. BLUEPRINTS) in this prompt. We'll match them in a separate pass once this one looks right.

CHANGE 2 — COMPACT VERTICAL SPACING

The section currently has roughly ~600+ pixels of vertical height for what's effectively a model name, a one-line tag, a two-line description, and one alternative card. Tighten as follows:

- Top padding of the section (above the new heading lockup): keep ~64px (this preserves the section break feel).
- Gap between the heading lockup hairline and the start of the model card content: 32px (was likely 64–80px).
- The "openai / gpt-5.5" model name: keep its Fraunces serif size, but reduce the gap below it to 8px (was likely 16–24px). The mono-caps "RECOMMENDED · DETERMINISTIC FALLBACK" tag sits directly under, then 20px gap to the description.
- The description paragraph ("Use the teacher/default model..."): constrain its max-width to ~560px so it wraps onto 2 tight lines instead of stretching wide. Keep the same font size and color.
- Gap between the description and the "-- ALTERNATIVES" sub-label: 28px (was likely 56px+).
- The alternatives card (gemini / gemini-2.5-flash · SHADOW ONLY): keep as-is but reduce its top margin by ~50%.
- Bottom padding of the section (below the alternatives card, before the next section): reduce to ~48px.

ALIGNMENT CHANGE
The entire section body (model name, tag, description, alternatives label, alternatives card) is currently centered. Switch to LEFT-aligned to match the new heading lockup. Left alignment by itself eliminates a lot of the "this section is huge" feeling — centered blocks always feel heavier.

   - Model name "openai / gpt-5.5": left-aligned, Fraunces serif, --accent terracotta color, keep existing size.
   - Mono caps tag below it: left-aligned.
   - Description paragraph: left-aligned, max-width 560px.
   - "-- ALTERNATIVES" sub-label: left-aligned, keep mono caps 10px --text-muted.
   - Alternatives card: left-aligned, NOT full width — width matches the description's 560px max-width.

CONSTRAINTS / WHAT NOT TO TOUCH
- Do not change font sizes other than promoting the section heading to 32px Fraunces.
- Do not change the model name's color — it's already terracotta, that stays.
- Do not change the alternatives card's internal design (the inset code-style box with "SHADOW ONLY" subtitle stays identical).
- Do not change the abstract decoration behind the section.
- Do not touch the I. INPUT or III. BLUEPRINTS sections in this pass.

VERIFICATION
1. Reload the results page. Confirm the RECOMMENDED MODEL section is visibly more compact — the entire section, hairline to hairline, should now fit comfortably in roughly half the vertical space it previously took.
2. Confirm the heading reads as a real heading: "II" small terracotta kicker, then "Recommended model" in Fraunces 32px, left-aligned, with a hairline below.
3. Confirm all body content (model name, tag, description, alternatives label, alternatives card) is left-aligned, not centered.
4. Confirm the description paragraph wraps onto ~2 lines instead of stretching wide.
5. Confirm the alternatives card width matches the description column width, not full container width.
6. Resize the viewport below 1024px. Heading lockup stays left-aligned, content remains left-aligned, nothing breaks or collides.