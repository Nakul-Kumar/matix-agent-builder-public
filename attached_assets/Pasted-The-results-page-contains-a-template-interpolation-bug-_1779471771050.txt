The results page contains a template-interpolation bug where the user's prompt gets converted to a Title-Case proper-noun string ("Research Assistant Agent That Summarises", "Software Engineer Agent For A", etc.) and injected as a literal substring into dozens of card titles and "Why selected" descriptions. Examples currently visible:

- Card titles: "Codex Software Engineer Agent For A Agent", "Claude Code Software Engineer Agent For A Agent", "OpenClaw Software Engineer Agent For A Agent"
- Search cards: "Search: Software Engineer Agent For A intent expansions", "Search: Software Engineer Agent For A agent"
- Why-selected lines: "Matches workflow planning for this Software Engineer Agent For A bundle."

Fix:
1. Locate the server-side function that builds the result card data from the user's prompt input. Most likely a TypeScript/Python module that takes the raw prompt and produces card titles, descriptions, and search expansions.
2. Identify the place where the prompt is being processed into a Title-Case noun phrase. It's likely a function that capitalises the first letter of each word in the entire prompt and uses the result as a substitute for {agent_slug} or {agent_name} placeholders.
3. Replace this logic with one of:
   - A short slug derived from the prompt (max 3 hyphenated words, lowercase, e.g. "research-assistant" or "software-engineer").
   - A generic placeholder ("this agent", "this bundle", "the requested agent") when no slug is needed.

4. For card TITLES, change the format to: "{runtime name} blueprint" — drop the agent-name injection entirely. So instead of "Codex Software Engineer Agent For A Agent", the title becomes "Codex blueprint", "Claude Code blueprint", "OpenClaw blueprint".

5. For "Why selected" lines, change the format to: "Matches {capability} for the requested bundle." Do not insert the prompt-derived noun phrase.

6. For search expansion cards, change the format to: "Search: {capability} expansion" or simply use the capability name as the title.

7. Verification: submit the prompt "Build a research assistant agent that summarises academic papers from PubMed, tracks citations across publications, and exports findings as annotated PDFs." and confirm that no card on the results page contains the substring "Research Assistant Agent That Summarises". Then submit "Build a software engineer agent for a Next.js app with GitHub, Postgres, and Playwright testing." and confirm no card contains "Software Engineer Agent For A". If either string appears anywhere, the fix has failed.

Report the file you changed and one before/after card title pair.