The textarea on the landing page currently loads with the example sentence "Build a software engineer agent for a Next.js app with GitHub, Postgres, and Playwright testing." set as its INITIAL VALUE. This means the user must select-all-and-delete it before typing their own input. The character counter also shows 96/1000 on initial page load, confirming the value is real text, not a placeholder.

Change it so the example appears as a faded placeholder hint that disappears the moment the user types or focuses-and-types.

Implementation:

1. Locate the textarea component / element. It is probably in src/App.tsx or a child component in src/ that contains the "Describe your agent" label.

2. Find where the textarea's value is initialised. It is likely either:
   - A useState initialiser like useState("Build a software engineer agent for a Next.js app...")
   - A defaultValue prop on the textarea element
   - A value prop bound to a state variable initialised to the example string.

3. Change it so:
   - The initial state value is an empty string: useState("")
   - The example sentence is moved to the placeholder attribute on the textarea: placeholder="Build a software engineer agent for a Next.js app with GitHub, Postgres, and Playwright testing."
   - Remove any defaultValue prop if present.

4. Verify the character counter behavior:
   - On initial page load the counter must read 0/1000.
   - As the user types, the counter updates live to match the value length.
   - When the user clears the textarea, the counter returns to 0/1000 and the placeholder reappears.

5. Style the placeholder so it reads as a hint, not active text:
   - color: #6E6359 (muted)
   - font-style: normal (same family as the textarea — do not italicise)
   - opacity: 1 (so the muted color is the only signal it's a placeholder)

6. The placeholder must:
   - Be visible the moment the page loads, in muted color, inside the textarea.
   - Disappear immediately when the user begins typing.
   - Reappear if the user clears the textarea back to empty.
   - Wrap to multiple lines if the textarea is narrower than the placeholder text.

7. Preserve the existing six prompt cards' click behavior — clicking a card still replaces the textarea value with that card's full insert prompt and updates the counter accordingly.

8. Preserve the Build agent button behavior — submission only fires when the textarea has non-empty content (this aligns with the empty-state handling from the other prompt I'll send next; for now just ensure the existing handler still works).

9. Do not change anything else: card content, layout, decoration, footer, hover states, fonts, or styling outside the textarea itself.

Verification after change:
- Reload the page in a fresh tab.
- The textarea is empty. The counter reads 0/1000.
- The example sentence is visible inside the textarea in muted color #6E6359 as a placeholder hint.
- Click into the textarea and start typing — the placeholder vanishes, the counter increments.
- Delete all typed content — the placeholder reappears and the counter returns to 0/1000.
- Click any of the six prompt cards — the textarea fills with that card's full prompt, the counter shows the new length, and the placeholder is hidden because the textarea now has a real value.