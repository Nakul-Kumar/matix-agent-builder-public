node_modules/
dist/
dist-server/
.env
.env.*
!.env.example
*.log

# Accidental shell-redirect artifacts (e.g. `git log > tatus`)
tatus
status

# Local design/iteration history — not part of the published product
attached_assets/

# Local agent memory — not part of the published product
.agents/
